# go-Ehendown

## Usage

```bash
Usage: go-Ehendown [options]

Options:
    -u, --URL <URL>             e-hentai post URL *Require
    -o, --out <path>            destination folder path
    -h, --help                  Show usage
```